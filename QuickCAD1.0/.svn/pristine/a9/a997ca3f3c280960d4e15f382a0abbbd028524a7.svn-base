using Max.BaseKit;
using IsolatorTarget.Models;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Authorization;

namespace IsolatorTarget.Controllers
{
    /// <summary>
    /// 系统设置控制器
    /// </summary>
    [Authorize]
    public class SystemSetController : Controller
    {
        private readonly IConfiguration _configuration;
        public SystemSetController(IConfiguration configuration)
        {
            _configuration = configuration;
        }

        public IActionResult Index()
        {
            bool flag = _configuration.GetValue<bool>("IsBizUsable");
            _configuration["IsBizUsable"] = "true";
            return View();
        }
    }
}